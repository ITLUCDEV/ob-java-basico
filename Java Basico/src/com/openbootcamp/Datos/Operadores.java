package com.openbootcamp.Datos;

public class Operadores {
    public static void main(String[] args) {

        // com.openbootcamp.Datos.Operadores

        // Aritmeticos

        // + - / * %

        int number1 = 5;
        int number2 = 2;

        int resultado = number1 + number2;
        int resultado2 = number1 - number2;
        int resultado3 = number1 * number2;
        double resultado4 = number1 / number2;

        // Logicos, relacion, comparacion, booleanos

        /*
        >  (mayor)
        >= (mayor o igual que)
        <  (menor)
        <= (menor o igual que)
        == (igual)
        != (distinto)
        && (and)
        || (or)

         */

        // com.openbootcamp.Datos.Operadores de asignacion

        /*
        =
        +=
        -=
        /=
        *=
        %=
         */

        // Incremento
        // ++
        number1++;

        // Decremento
        // --
        number2--;

        // Concatenación
        // +
    }
}
